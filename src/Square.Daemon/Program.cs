using Square.Contracts;

Console.Out.WriteLine($"square-daemon bootstrap — protocol {ContractVersions.DraftV1}");
Console.Out.WriteLine("The durable daemon and named-pipe server are intentionally gated behind SP00-T03/G0 and SP02.");
return 0;
