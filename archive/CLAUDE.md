# Archive Context for Claude

Frozen pre-fork documents, plans, source, and tooling, kept for trace/history only. Read root
`CLAUDE.md` and `STATUS.md` first; neither this directory nor its own nested context pairs grant
implementation authority.

## File map

| Path | Purpose |
|---|---|
| `AGENTS.md`, `CLAUDE.md` | This context pair |
| `docs/` | Former `docs/` documentation, evidence, validation, and inventories |
| `plans/` | Superseded plans, specifications, and archive-tidy packet |
| `src/` | Former implementation trees, grouped by responsibility/type |
| `tooling/` | Former root toolchain, scripts, and CI |
| `artifacts/` | Gitignored scratch output, unchanged and untracked |
