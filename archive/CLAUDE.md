# Archive Context for Claude

Frozen pre-fork implementation content, kept for trace/history only. Read root `CLAUDE.md` and
`STATUS.md` first; neither this directory nor its own nested context pairs grant implementation
authority.

## File map

| Path | Purpose |
|---|---|
| `AGENTS.md`, `CLAUDE.md` | This context pair |
| `src/`, `ui/`, `vscode/`, `prototypes/`, `contracts/`, `build/`, `tests/`, `sqorch/` | Former repository root directories, moved here unchanged on 2026-08-09 |
