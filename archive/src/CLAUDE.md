# Archived Source Context for Claude

Frozen pre-fork implementation trees, retained for trace/history only. Read root `CLAUDE.md` and
`STATUS.md` first; this tree does not grant implementation authority.

## File map

| Path | Purpose |
|---|---|
| `AGENTS.md`, `CLAUDE.md` | Directory context pair |
| `dotnet/`, `sqorch/`, `contracts/`, `prototypes/`, `ui/`, `vscode/`, `tests/`, `build/` | Grouped former implementation and test trees |
