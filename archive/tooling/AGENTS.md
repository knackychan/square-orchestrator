# Archived Tooling Context

This directory contains the frozen pre-fork toolchain, convenience scripts, and CI configuration.
It is trace/history only and is not the live fork toolchain.

## File map

| Path | Purpose |
|---|---|
| `AGENTS.md`, `CLAUDE.md` | Directory context pair |
| `.github/` | Former CI workflows |
| `.editorconfig`, `.nvmrc`, `Directory.Build.props`, `Directory.Packages.props`, `NuGet.Config`, `SquareOrchestrator.slnx`, `global.json` | Former .NET and editor configuration |
| `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `tsconfig.json` | Former Node/pnpm/TypeScript configuration |
| `build.ps1`, `dev.ps1`, `format.ps1`, `package.ps1`, `test.ps1` | Former convenience scripts |
| `THIRD_PARTY.md` | Former dependency inventory |
