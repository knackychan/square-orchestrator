# Archived Tooling Context for Claude

Frozen pre-fork toolchain, scripts, and CI configuration, retained for trace/history only. It is
not the active fork toolchain.

## File map

| Path | Purpose |
|---|---|
| `AGENTS.md`, `CLAUDE.md` | Directory context pair |
| `.github/` | Former CI workflows |
| Root files | Former .NET, Node, pnpm, TypeScript, dependency, and PowerShell toolchain files |
