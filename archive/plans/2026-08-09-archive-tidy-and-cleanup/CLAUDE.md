# Archive Tidy and Cleanup Plan Context for Claude

This packet plans the physical reorganisation of pre-fork artifacts into a sorted `archive/` and the
cleaning of the root `docs/` tree to fork-era state. It is planning only. It must be accepted and
activated by the owner before any file move runs; this plan's task list does not self-activate.

Root `STATUS.md` remains authoritative for what may run.

## File map

| Path | Purpose |
|---|---|
| `AGENTS.md`, `CLAUDE.md` | Directory context pair |
| `PLAN.md` | Execution manifest: target layout, move manifest, context-pair updates, validation, acceptance criteria |
