# M0 Bootstrap Context

This packet is documentation-only. It may initialize Git and create or verify the files named below.
It may not create source code, package metadata, dependencies, installers, extensions, runtime
state, or launch agents.

## Reading order

1. Root `AGENTS.md`, `SPEC.md`, and `STATUS.md`.
2. `PACKET.md`.
3. `BUILD.md`.
4. `BUILD-TASKS.md`.
5. `STATE.md`.

## File map

| Path | Purpose |
|---|---|
| `AGENTS.md`, `CLAUDE.md` | Packet context pair |
| `PACKET.md` | M0 authority boundary and acceptance contract |
| `BUILD.md` | Bootstrap decision register |
| `BUILD-TASKS.md` | Exact documentation tasks and commits |
| `STATE.md` | Progress ledger; never authority |

