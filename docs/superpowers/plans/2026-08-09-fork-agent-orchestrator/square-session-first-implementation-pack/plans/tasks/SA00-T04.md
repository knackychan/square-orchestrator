# SA00-T04 — Import authority and activate the architecture amendment

- Phase: SA00 — Fork adoption and reproducible baseline
- Gate contribution: A0
- Prerequisite: reviewed SA00-T03
- Scope: documentation/authority only

## 1. Objective

Copy the owner-approved session-first Square plan and UI references into the downstream repository as immutable, hash-addressed authority material. Record the architectural pivot from the unaccepted .NET/global-dashboard line to the AO-based session-first product. Remove ambiguity over which plan controls undispatched work.

No product source is implemented in this task.

## 2. Authority pack inputs

Expected source package files:

```text
plans/MASTER_IMPLEMENTATION_PLAN.md
docs/ARCHITECTURE_AMENDMENT.md
docs/SESSION_DOMAIN_MODEL.md
docs/ROLE_ROUTING_MODEL_SELECTION.md
docs/PERSISTENCE_AND_EVENTS.md
docs/API_AND_EXECUTION_FACADE.md
docs/SESSION_FIRST_UI_SPEC.md
docs/UPSTREAM_GOVERNANCE.md
docs/TEST_AND_RELEASE_STRATEGY.md
source-map/AO_SOURCE_PLACEMENT_MAP.md
ui/square-session-workspace-rounded-reference.html
ui/square-session-workspace-rounded-reference-README.md
schemas/*.schema.json
fixtures/*.json
```

The source pack's own `MANIFEST.sha256` must verify before import when present.

## 3. Required owner decision

Before import, the owner must explicitly accept or amend:

- AO `v0.12.1` as pinned base;
- maintained downstream fork;
- session-first top-level UX;
- deterministic Task Manager;
- bounded model roles;
- QUICK before PLANNED;
- per-role AUTO/PREFERRED/PINNED routing;
- requested/resolved/actual identity separation;
- rounded session-first HTML as visual direction;
- loopback REST/SSE/WebSocket retained for MVP;
- Windows x64 first;
- telemetry/updater/data isolation decisions from SA00-T03.

Do not infer owner acceptance from a coding agent starting the task. Capture the decision explicitly.

## 4. First response required

Report:

1. current commit/branch/clean state;
2. SA00-T01..T03 accepted receipt hashes;
3. source pack path and manifest verification result;
4. exact import mapping;
5. owner acceptance wording/date;
6. collisions with existing `docs/square/authority` files;
7. any contradictory accepted document.

## 5. Destination layout

Copy source bytes into stable names:

```text
docs/square/authority/
  00-architecture-amendment.md
  01-master-session-first-plan.md
  02-session-domain-model.md
  03-role-routing-model-selection.md
  04-persistence-and-events.md
  05-api-and-execution-facade.md
  06-session-first-ui-spec.md
  07-upstream-governance.md
  08-test-and-release-strategy.md
  09-ao-source-placement-map.md
  10-approved-rounded-session-ui.html
  11-approved-rounded-session-ui-readme.md
  contracts-draft/
    *.schema.json
  fixtures/
    *.json
  AUTHORITY_MANIFEST.sha256
  AUTHORITY_INDEX.json
```

Also add:

```text
docs/square/adr/ADR-SA00-004-session-first-ao-foundation.md
docs/square/upstream/supersession-register.md
docs/square/receipts/SA00-T04.json
docs/square/evidence/SA00-T04/<UTC-stamp>/**
```

Do not overwrite an existing file with different bytes. A collision is a STOP condition pending an explicit amendment/versioned filename.

## 6. Supersession register

The register must distinguish:

### Preserved behavioral research

- daemon owns work;
- UI closure is harmless;
- terminal output is untrusted;
- deterministic lifecycle classification;
- typed interactions;
- controller/writer authority;
- bounded model sessions;
- acceptance/evidence/memory principles;
- original docked-terminal design language.

### Retired production implementation decisions

- custom C#/.NET daemon;
- WPF/WebView2 shell;
- custom named-pipe RPC as immediate production path;
- custom .NET SQLite layer;
- custom ConPTY stack as the application foundation;
- global fleet/dashboard as the primary UX.

### Current production direction

- AO Go/Electron/SQLite/ConPTY/worktree platform;
- Square Session as top-level object;
- task manager and model roles inside each session;
- dynamic docked role histories;
- per-role model routing.

Historical documents are not deleted. They are marked research/superseded, not silently rewritten.

## 7. Authority index

`AUTHORITY_INDEX.json` includes for each file:

- relative path;
- SHA-256;
- document role;
- precedence order;
- status (`accepted`, `reference`, `draft-contract-fixture`, `superseded-research`);
- owner acceptance timestamp;
- source pack path/hash;
- whether implementation workers may amend it (normally no).

The HTML is `accepted visual reference`; written contracts override it on behavior/security/authority.

## 8. Hashing

Use:

```powershell
& "<PACK>\scripts\capture-authority-hashes.ps1" `
  -AuthorityDirectory "<REPO>\docs\square\authority"
```

Then verify every line by recomputing hashes. The manifest must not include itself.

## 9. Allowed writes

```text
docs/square/authority/**
docs/square/adr/ADR-SA00-004-session-first-ao-foundation.md
docs/square/upstream/supersession-register.md
docs/square/evidence/SA00-T04/**
docs/square/receipts/SA00-T04.json
```

## 10. Forbidden actions

- no backend/frontend/package changes;
- no source code generated from draft schemas yet;
- no database migration;
- no UI implementation;
- no deletion of older research;
- no silent reconciliation of contradictory owner documents;
- no conversion/reformatting of imported files;
- no replacement of source URLs/hashes with memory or inference;
- no dispatch of later tasks during this task.

## 11. Validation

- source pack manifest passes;
- destination hash equals source hash for each imported file;
- JSON schemas parse and fixtures validate against their referenced schemas;
- HTML opens/parses and has no accidental absolute machine paths/secrets;
- authority manifest verifies;
- index paths/hashes/statuses are complete;
- supersession register names all previous plan lines known to the repository;
- tracked changes are documentation only;
- architecture dependency/source baseline remains unchanged.

## 12. Evidence

```text
docs/square/evidence/SA00-T04/<UTC-stamp>/
  owner-decision.json
  source-pack-manifest.txt
  import-map.json
  validation.json
  changed-files.json
  summary.json
  manifest.sha256
```

`owner-decision.json` must contain the owner's wording or a direct approved decision summary, not a coding agent's assumption.

## 13. Commit and receipt

Suggested commit:

```text
SA00-T04: adopt session-first Square authority
```

Receipt records:

- source and destination manifests;
- owner decision;
- superseded/preserved/current directions;
- changed files;
- no product code;
- next eligible task: SA00-T05 gate review only.

## 14. Mandatory STOP conditions

Stop when:

1. owner acceptance is absent or ambiguous;
2. source pack manifest fails;
3. destination contains same path with different bytes;
4. accepted documents contradict each other materially;
5. an authority file contains secrets or private absolute paths that should not be committed;
6. JSON schemas/fixtures are invalid;
7. import requires editing product source;
8. history would be deleted rather than superseded;
9. the agent is asked to decide a parked product/security policy;
10. the approved UI reference differs from the owner-selected rounded session-focused version.

## 15. Acceptance criteria

- **SA00-AC-20:** one explicit authority hierarchy controls future work.
- **SA00-AC-21:** session-first AO architecture amendment is owner accepted.
- **SA00-AC-22:** every authority/reference byte is hash-addressed and indexed.
- **SA00-AC-23:** old research is preserved but cannot silently control new implementation.
- **SA00-AC-24:** no product source changed.
- **SA00-AC-25:** A0 has a complete review input set.

## 16. Final report

Return owner decision, imported paths/hashes, authority precedence, superseded research summary, validation, evidence manifest, commit, and SA00-T05 readiness.
