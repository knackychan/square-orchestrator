# Upstream Records

Record the exact Agent Orchestrator tag, commit, tree, remotes, release evidence, baseline behavior, and every later upstream synchronization here. Never rewrite an old upstream-sync record.
